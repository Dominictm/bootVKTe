# PORT, WEB_ADMIN_LOGIN and WEB_ADMIN_PASSWORD are optional.
# If left blank (or this file is missing entirely), the panel falls back to
# the default login Admin / password 123 — change it from the panel itself
# (section "Логин и пароль панели" on the settings page) after first login.
PORT=8546
WEB_ADMIN_LOGIN=
WEB_ADMIN_PASSWORD=
